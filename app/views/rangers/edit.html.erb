<h1>Edit a Ranger</h1>

<%= form_for @ranger do |r| %>
	<div>
		<%= r.label :name %>
		<%= r.text_field :name %>
	</div>
	<div>
		<%= r.collection_check_boxes :park_ids, Park.all, :id, :parkname %>
	</div>
	<%= r.submit %>
<% end %>