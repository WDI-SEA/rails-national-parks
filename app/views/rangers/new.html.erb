
<h2>Add a new Park Ranger</h2>

<%= form_for @ranger do |r| %>
  <div>
    <%= r.label :name %>
    <%= r.text_field :name %>    
  </div>

  <%= r.collection_check_boxes :park_ids, Park.all, :id, :name %>

  <%= r.submit %>

<% end %>

<%= link_to "Back to All Park Rangers", rangers_path %>